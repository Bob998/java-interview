# 微服务架构面试问答
微服务适合业务复杂、团队较大、迭代快的场景，拆分时应按业务领域循序渐进，并优先搭好注册中心、配置中心、网关、监控等基础设施；JavaGuide则把微服务相关基础设施拆得更细，覆盖网关、配置推送、RPC、注册发现、负载均衡等面试高频点。

---

## 一、基础认知

### Q1：什么是微服务？

**答：**  
微服务是一种**架构风格**，核心不是“把服务拆小”这么简单，而是围绕**业务边界**把系统拆成多个**独立开发、独立部署、独立扩缩容**的服务。每个服务只负责自己的一块业务能力，服务之间通过 RPC、HTTP 或消息队列通信。小林coding把它概括得很清楚：**分布式的核心是机器的拆分和协同，微服务的核心是业务逻辑和业务边界的拆分**。

### Q2：微服务和分布式有什么区别？

**答：**  
这两个概念相关，但不完全一样。  
分布式更强调**部署层面**，即把系统部署到多台机器上协同工作；微服务更强调**业务架构层面**，即按业务边界拆服务。单体系统也可以是分布式部署，比如一套代码部署多实例；而微服务天然通常也是分布式的，因为拆成几十上百个服务后，不可能只跑在一台机器上。

### Q3：微服务一定比单体好吗？

**答：**  
不一定。小林coding明确提到：微服务更适合**业务复杂、团队规模大、需要快速迭代**的场景；如果只是小项目、小团队，单体可能更合适。因为微服务虽然解决了扩展性和协作效率问题，但会引入注册发现、配置管理、网络调用、分布式事务、监控治理等一整套复杂度。面试里要强调：**架构选型一定是权衡，不是越“高级”越好。**

### Q4：单体架构和微服务架构的核心区别是什么？

**答：**  
单体架构是**一个应用、一个部署单元**，代码集中、调用基本都是本地方法调用，开发测试部署简单；微服务是**多个服务、多个部署单元**，各服务职责更清晰，可以独立发布和扩容，但代价是要处理远程调用、网络故障、数据一致性、服务治理等问题。面试里可以总结成一句：  
**单体的优点是简单，微服务的优点是灵活；单体的问题在扩展性，微服务的问题在分布式复杂度。**

---

## 二、服务拆分

### Q5：微服务怎么拆？

**答：**  
拆分的核心原则是**按业务领域拆，不要按技术层拆**。小林coding给的思路是：  
先梳理现有系统的业务模块和依赖关系，再按业务领域划分服务边界，遵循 DDD 的思想，比如用户、商品、订单、支付、库存这些职责清晰的模块，就适合拆成不同服务。优先拆**业务独立、变更频繁、需要单独扩容**的模块。

### Q6：拆分微服务有哪些原则？

**答：**  
面试里通常讲这几个原则：

1. **高内聚、低耦合**：一个服务只做一类业务。  
2. **单一职责**：避免一个服务承担太多职责。  
3. **服务边界稳定**：边界不要今天这么拆、明天又改。  
4. **数据归属清晰**：服务最好管理自己的数据。  
5. **避免共享数据库作为常态**：否则只是“伪微服务”。  
6. **先粗拆，再细化**：不要一开始就拆得特别碎。  

小林coding特别强调要**循序渐进**，不是“一上来就全拆”。

### Q7：从单体迁移到微服务，怎么落地比较稳？

**答：**  
一个很标准的回答是：**绞杀者模式**。  
小林coding给出的建议是：先用新服务承接新增功能，老功能逐步迁移；先拆一个相对独立、风险较低的模块试水，同时把**注册中心、配置中心、网关、监控**这些基础设施先搭起来，等第一批服务跑稳，再逐步拆其他模块。这样改造风险更低，也更符合真实工程实践。

### Q8：数据库怎么拆？

**答：**  
数据库拆分通常比服务拆分更难。小林coding提到，一个比较稳妥的路径是：

- 先做**逻辑拆分**：不同服务访问不同 schema 或不同表；
- 边界稳定后，再做**物理拆分**：每个服务拥有自己的数据库；
- 原来单库事务能解决的问题，拆分后就要面对**分布式事务或最终一致性**。

所以面试里最好讲：**数据库拆分要晚于业务边界稳定，不能一开始就激进物理拆库。**

---

## 三、服务通信

### Q9：微服务之间怎么通信？

**答：**  
常见有两大类：

1. **同步调用**：HTTP/REST、RPC。  
2. **异步调用**：消息队列。  

同步调用适合实时性要求高的链路，异步调用适合削峰、解耦、重试、最终一致性。JavaGuide 在 Dubbo 专题里把 RPC 这条线讲得很清楚：RPC 的价值不只是远程调用本身，还包括**负载均衡、服务注册发现、服务治理**等配套能力。

### Q10：HTTP 和 RPC 怎么选？

**答：**  
一般这么答：

- **HTTP/REST**：通用性强、调试方便、跨语言友好，适合对外接口或跨团队协作。  
- **RPC**：调用语义更像本地方法，性能通常更好，接口约束更强，适合内部服务调用。  

如果你是 Java 技术栈内部服务，很多场景会优先选 RPC；如果要开放给前端、第三方或多语言系统，HTTP 更常见。JavaGuide 对 Dubbo 的定位也是：它是一款高性能、轻量级的 Web 和 RPC 框架，提供高性能 RPC 调用、负载均衡、自动注册发现、治理等能力。

### Q11：Dubbo 这类 RPC 框架解决了什么问题？

**答：**  
面试里可以从“远程调用不只是发请求”这个角度答。JavaGuide总结的点包括：

- **负载均衡**：同一个服务多实例时选哪台；
- **服务自动注册与发现**：消费者不用写死地址；
- **服务治理**：调用链、流量调度、监控统计；
- **智能容错**：失败重试、集群策略等。  

所以 RPC 框架的价值不只是“通信”，而是把微服务调用的一整套基础能力封装起来。

---

## 四、注册中心与服务发现

### Q12：为什么微服务需要注册中心？

**答：**  
因为微服务实例是动态变化的。服务会扩容、缩容、重启、迁移，如果消费者把提供者地址写死，维护成本和故障率都会非常高。注册中心的作用就是：**服务启动时注册自己，消费者从注册中心订阅可用实例列表**。JavaGuide 在 Dubbo 架构里也明确提到：`Provider` 会向注册中心注册自己提供的服务，`Consumer` 会向注册中心订阅所需服务，注册中心返回地址列表给消费者。

### Q13：服务注册与发现的流程是什么？

**答：**  
标准流程是：

1. 服务提供者启动并向注册中心注册。  
2. 消费者启动后订阅目标服务。  
3. 注册中心把可用实例列表推送或返回给消费者。  
4. 消费者本地缓存实例列表并结合负载均衡策略发起调用。  
5. 实例上下线时，注册中心通知消费者更新地址。  

这个回答和 Dubbo 架构中的 Provider、Consumer、Registry 角色关系是一致的。

### Q14：注册中心挂了怎么办？

**答：**  
面试里可以分两层答：

- **短期**：很多客户端会缓存服务列表，注册中心短时不可用，不一定立刻影响所有调用。  
- **长期**：如果无法续约、无法感知新实例、旧实例状态不更新，服务发现会逐渐失真，风险会放大。  

所以注册中心本身必须**高可用集群部署**，而且客户端通常要有**本地缓存和容错机制**。这一点和 JavaGuide 对配置中心“自身也要高可用部署”的要求是同一个治理思路。

---

## 五、负载均衡

### Q15：为什么微服务要做负载均衡？

**答：**  
因为同一个服务通常会部署多个实例做高可用和水平扩展，调用方需要在多个实例之间分发流量。JavaGuide 在 Dubbo 专题里把这点讲得很直接：负载均衡解决的是**“同一个服务部署在不同机器时该调用哪一台”**的问题。

### Q16：常见负载均衡策略有哪些？

**答：**  
常见有：

- **随机**：实现简单，整体较均匀；
- **轮询**：每次依次选择；
- **一致性哈希**：相同参数尽量落同一实例，适合有粘性需求的场景。  

JavaGuide 在 Dubbo SPI 扩展部分也提到，`LoadBalance` 是可扩展点，可以根据配置动态切换不同策略。

### Q17：客户端负载均衡和服务端负载均衡的区别？

**答：**  
客户端负载均衡是**消费者自己拿到实例列表后做选择**，比如 Dubbo/Ribbon 这类模式；服务端负载均衡是**先打到统一入口，再由入口转发**，比如 Nginx、网关层。  
微服务内部 RPC 场景里，客户端负载均衡很常见；外部流量入口一般走网关或反向代理。

---

## 六、API 网关

### Q18：为什么微服务需要 API 网关？

**答：**  
因为客户端不应该直接面对几十上百个后端服务。网关统一承担以下职责：

- 路由转发  
- 身份认证  
- 限流熔断  
- 负载均衡  
- 日志监控  
- 协议转换  

JavaGuide 的 API 网关专题明确把这些列为网关核心能力。

### Q19：Spring Cloud Gateway 的核心概念有哪些？

**答：**  
三个核心概念：

- **Route**：路由，决定把请求转到哪里；
- **Predicate**：断言，决定这个请求匹配哪条路由；
- **Filter**：过滤器，在请求前后做处理。  

这是 JavaGuide 对 Gateway 核心模型的标准总结。

### Q20：为什么现在更推荐 Spring Cloud Gateway，而不是 Zuul 1.x？

**答：**  
JavaGuide明确建议：Spring Cloud 新项目不要再选 Zuul 1.x，推荐直接使用 Spring Cloud Gateway。原因是：

- Zuul 1.x 基于 **Servlet，同步阻塞**；
- Spring Cloud Gateway 基于 **WebFlux + Netty，异步非阻塞**；
- 少量线程即可处理更多并发连接，资源利用率更高，还具备背压能力。

### Q21：Gateway 的过滤器分哪几类？

**答：**  
按请求生命周期分：

- **Pre**：请求转发前处理，比如鉴权、参数校验、日志；
- **Post**：响应返回后处理，比如改响应头、记录耗时。  

按作用范围分：

- **GatewayFilter**：局部过滤器，作用在某条路由上；
- **GlobalFilter**：全局过滤器，作用于所有请求。

### Q22：Gateway 支持动态路由吗？

**答：**  
支持。JavaGuide 介绍了一种常见做法：基于 **Nacos 注册中心 / 配置中心**感知服务元数据变化，动态调整路由规则，从而避免改配置后重启网关。这个点在面试里很加分，因为它说明你不只是会配静态路由，还理解线上网关的动态化能力。

---

## 七、配置中心

### Q23：为什么微服务一定要配配置中心？

**答：**  
JavaGuide给的理由非常完整。微服务服务多了以后，服务地址、数据库参数、开关配置都会增多，传统配置文件方式有几个明显问题：

- 不能动态更新，改完通常要重新发布；
- 敏感配置容易泄露；
- 多环境、多集群配置分散，难统一管理；
- 缺乏权限控制和审计；
- 一般还需要重启才能生效。  

所以配置中心本质上解决的是**动态化、集中化、可审计、可灰度**的问题。

### Q24：Apollo、Nacos、Spring Cloud Config 怎么选？

**答：**  
按照 JavaGuide 的建议：

- **只需要配置中心**：Apollo 或 Nacos；
- **同时要配置中心 + 服务发现**：Nacos；
- **Spring Cloud 生态且追求简单**：Spring Cloud Config；
- **Kubernetes 环境**：优先结合 K8s 原生方案。  

另外，JavaGuide也明确评价 Apollo 的配置管理功能更完善，Nacos 上手更简单，而且配置中心和注册发现二合一。

### Q25：配置中心常见推送机制有哪些？

**答：**  
JavaGuide总结了三类：

- **推模式**：实时性高，但服务端连接维护成本高；
- **拉模式**：简单，但会有无效轮询和延迟；
- **长轮询**：主流折中方案。  

同时它给了两个典型实现：

- **Apollo**：HTTP 长轮询；
- **Nacos 2.x**：gRPC 长连接双向流。  

这题面试非常高频，尤其问到“配置为什么能实时生效”的时候很好用。

### Q26：配置中心除了存配置，还要具备什么能力？

**答：**  
JavaGuide列的“必备功能清单”很适合直接背：

- 权限控制  
- 审计日志  
- 版本管理  
- 灰度发布  
- 多环境隔离  
- 高可用部署  

面试里一句话概括：**配置中心不只是个 KV 存储，而是配置治理平台。**

---

## 八、容错与稳定性

### Q27：微服务为什么更容易出现雪崩？

**答：**  
因为微服务调用链更长、依赖更多，任意一个下游抖动都有可能层层放大。JavaGuide在 Gateway 专题里提到，响应式模型的一项优势就是**背压机制**，当下游处理能力不足时可以自动调节上游请求速率，目的就是防止雪崩。这说明微服务里雪崩本质上是**依赖放大 + 流量失控**的问题。

### Q28：熔断、降级、限流分别是什么？

**答：**  
这是微服务稳定性三件套：

- **限流**：先控制流量入口，避免系统被打爆；  
- **熔断**：发现下游异常率高或超时严重时，短时间内直接拒绝请求，避免级联故障；  
- **降级**：核心链路保住，非核心能力暂时关闭或返回兜底数据。  

JavaGuide在网关选型表里也提到，Spring Cloud Gateway 可以集成 Resilience4j 来做限流熔断。

### Q29：微服务高可用一般怎么做？

**答：**  
标准答案一般有这几个层面：

- 服务多实例部署  
- 注册中心 / 配置中心高可用  
- 网关统一入口  
- 限流、熔断、降级  
- 超时、重试、隔离  
- 监控告警和快速回滚  

小林coding在单体改微服务时也强调，基础设施里**注册中心、配置中心、网关、监控**要先搭起来，这本身就是高可用体系的基础。

---

## 九、分布式事务与数据一致性

### Q30：微服务里为什么事务变难了？

**答：**  
因为单体里一个本地事务就能覆盖多个表操作，而微服务拆开后，数据分散在不同服务甚至不同数据库里，原来靠数据库 ACID 保证的一致性不再天然成立。小林coding在数据库拆分部分就点到了：原来用事务能解决的问题，拆开后就要面对**分布式事务或者最终一致性**。

### Q31：分布式事务有哪些常见方案？

**答：**  
JavaGuide 的分布式事务专题给出的主流方案包括：

- **2PC 两阶段提交**
- **3PC 三阶段提交**
- **TCC 补偿事务**
- **Saga 编排模式**
- **本地消息表**
- **事务消息**  

面试里通常要补一句：互联网业务里，很多场景最终追求的是**最终一致性**，而不是绝对强一致。

### Q32：这些分布式事务方案怎么选？

**答：**  
可以这样答：

- **强一致要求高**：考虑 2PC / TCC，但复杂度和性能成本更高；
- **长流程业务**：适合 Saga；
- **业务异步化明显、能接受最终一致**：本地消息表、事务消息更常见。  

真实业务里，**最终一致 + 幂等 + 重试 + 补偿**是更常见的工程解。这个回答虽然是归纳，但与 JavaGuide 列出的方案谱系一致。

### Q33：微服务里怎么保证幂等？

**答：**  
常见手段：

- 请求唯一 ID  
- 数据库唯一约束  
- Redis 去重  
- 状态机控制  
- 消息消费去重  

虽然这部分不是小林或 JavaGuide 该页的逐条枚举，但它和“事务消息 / 本地消息表 / 最终一致性”的工程落地是配套出现的。面试回答时可以落到你自己的项目经验上，会更有说服力。

---

## 十、监控、治理与可观测性

### Q34：微服务监控为什么比单体更重要？

**答：**  
因为一个请求可能会经过网关、鉴权、订单、库存、支付等多个服务，出了问题以后如果没有日志、指标、链路追踪，就很难快速定位。JavaGuide 在 Dubbo 架构中单独列了 `Monitor` 角色，用于统计调用次数和调用时间；小林coding也把“监控”列为从单体改微服务时必须优先建设的基础设施。

### Q35：微服务治理包含哪些内容？

**答：**  
可以从这几个方面回答：

- 服务注册与发现  
- 负载均衡  
- 配置管理  
- 流量治理  
- 限流熔断降级  
- 可观测性  
- 灰度发布 / 回滚  
- 安全鉴权  

JavaGuide对 Dubbo 的定义里就包含“运行期流量调度”和“可视化服务治理与运维”，这两个词很适合面试里直接用。

---

## 十一、开放追问题

### Q36：微服务拆得越细越好吗？

**答：**  
不是。拆得过细会带来更多网络调用、更多部署单元、更多运维成本、更多一致性问题。正确做法是按业务边界拆到**团队能管理、系统能治理、链路复杂度可控**的粒度。小林coding的观点本质上就是：**不要为了微服务而微服务，拆分要循序渐进。**

### Q37：微服务最难的点是什么？

**答：**  
我会回答三个最核心的：

1. **服务边界划分**  
2. **数据一致性**  
3. **分布式治理复杂度**  

因为真正难的不是把代码拆成多个项目，而是拆完以后系统还能稳定、高效、可演进。这个判断和小林coding对数据库拆分、分布式事务、基础设施建设的强调是一致的。

### Q38：面试官问“你们项目为什么要上微服务”，怎么答？

**答：**  
一个很稳的回答模板是：

> 我们上微服务不是为了追求新架构，而是因为业务复杂度、团队协作和扩展性已经超过单体的舒适区。单体下模块耦合重、发布影响面大、热点模块无法独立扩容，所以我们按业务领域逐步拆分。拆分过程中优先建设注册中心、配置中心、网关和监控体系，并通过 RPC、负载均衡、配置中心、限流熔断和最终一致性方案解决拆分后的治理问题。

这个模板和小林coding、JavaGuide两边的主线都对得上。

---

# 最后给你一版“速记提纲”

面试时你至少要能顺着讲出这 10 个关键词：

**业务拆分、注册发现、RPC/HTTP、负载均衡、API 网关、配置中心、限流熔断降级、分布式事务、幂等、监控治理。**

这些正好对应了小林coding提到的微服务拆分路径和 JavaGuide 拆开的各个基础设施专题。
