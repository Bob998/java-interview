# 数据库面试问答知识点
> 本文以 **MySQL 为主、Redis 为辅**，按照 **“面试问答 + 高频追问 + 体系化理解”** 的方式整理，尽量覆盖后端岗位数据库面试中的常见高频点。  
> 适用场景：后端开发 / Java 后端 / 高并发系统 / 微服务 / 中高级面试复习。  
> 阅读建议：先通读建立体系，再重点背诵“索引、事务、MVCC、锁、日志、SQL 执行、Redis、分库分表”八大板块。

---

# 一、数据库总览

## Q1：数据库面试通常考什么？

**答：**

数据库面试一般围绕以下几个层次展开：

### 1）基础认知
- 什么是数据库
- 关系型数据库与非关系型数据库区别
- 为什么项目里要同时用 MySQL 和 Redis

### 2）MySQL 核心
- 索引原理
- B+Tree
- 聚簇索引、二级索引、回表、覆盖索引
- 联合索引、最左匹配、索引失效
- 事务、ACID
- 隔离级别、脏读、不可重复读、幻读
- MVCC
- 锁机制（记录锁、间隙锁、Next-Key Lock）
- undo log、redo log、binlog
- SQL 执行流程
- 慢 SQL 优化

### 3）Redis 核心
- Redis 为什么快
- 数据结构与典型使用场景
- 持久化
- 缓存穿透、击穿、雪崩
- 分布式锁
- 主从、哨兵、集群
- BigKey、HotKey

### 4）架构设计层
- 读写分离
- 主从复制
- 分库分表
- 缓存与数据库一致性
- 高并发下的数据库瓶颈治理

---

## Q2：关系型数据库和非关系型数据库有什么区别？

**答：**

### 关系型数据库（如 MySQL）
特点：
- 数据按表结构组织
- 支持 SQL
- 支持事务
- 强调一致性
- 适合复杂查询、关联查询

典型场景：
- 用户表、订单表、支付表、库存表、账户表

### 非关系型数据库（如 Redis）
特点：
- 不强调固定表结构
- 更强调高性能和灵活存储
- 通常不擅长复杂 join
- 更适合缓存、计数、队列、限流等场景

典型场景：
- 缓存
- 排行榜
- 会话存储
- 分布式锁
- 热点数据承接

### 一句话总结
- **MySQL 负责“存得准、查得全”**
- **Redis 负责“查得快、抗得住高并发”**

---

## Q3：为什么项目里通常 MySQL 和 Redis 一起使用？

**答：**

因为两者解决的问题不同：

- **MySQL**：负责持久化存储和复杂查询，是核心数据源
- **Redis**：负责热点承接和高并发访问，是性能加速层

常见组合方式：
- 先查 Redis，Redis 没有再查 MySQL
- MySQL 更新后删除或更新 Redis
- Redis 存缓存、计数器、排行榜、锁、短期状态
- MySQL 存最终真实数据

### 面试加分说法
Redis 不是 MySQL 的替代品，而是 MySQL 的性能补充层。  
用了 Redis 后，系统会更快，但也会引入新的问题，比如：
- 缓存一致性
- 缓存雪崩
- 热点 Key
- 过期策略设计

---

# 二、MySQL 基础

## Q4：MySQL 常见存储引擎有哪些？为什么面试主要问 InnoDB？

**答：**

常见存储引擎：
- InnoDB
- MyISAM

### InnoDB 的特点
- 支持事务
- 支持行锁
- 支持外键
- 支持 MVCC
- 支持崩溃恢复

### MyISAM 的特点
- 不支持事务
- 主要使用表锁
- 更早期使用较多，现在业务核心场景里较少使用

### 为什么面试主要问 InnoDB？
因为现代业务系统大多数核心表都使用 InnoDB，它既支持事务，又支持高并发控制，是 MySQL 面试的重点。

---

## Q5：数据库三大范式是什么？

**答：**

### 第一范式（1NF）
字段不可再分，保证原子性。

例如：
- 错误设计：`phones = "123,456"`
- 正确设计：每个字段只存一个值

### 第二范式（2NF）
在满足 1NF 的基础上，非主属性必须完全依赖主键，不能只依赖联合主键的一部分。

### 第三范式（3NF）
在满足 2NF 的基础上，非主属性不能传递依赖主键。

### 面试中的理解
范式的目标是：
- 减少冗余
- 避免更新异常
- 提高一致性

### 但实际工程中
为了查询性能，也可能适当做**反范式设计**，例如冗余部分字段，减少 join。

---

# 三、索引篇（高频重中之重）

## Q6：什么是索引？

**答：**

索引是帮助数据库**快速定位数据**的数据结构。  
本质上，它的目标是减少磁盘 IO 和扫描行数，提高查询效率。

如果没有索引，数据库很多时候只能：
- 全表扫描
- 一行一行比对条件

有了索引后，可以：
- 快速查找
- 支持范围查询
- 支持排序和分组优化

---

## Q7：MySQL 常见索引类型有哪些？

**答：**

### 按数据结构分
- B+Tree 索引
- Hash 索引（某些场景/引擎）
- Fulltext 全文索引

### 按物理存储分
- 聚簇索引
- 二级索引（辅助索引）

### 按字段特性分
- 主键索引
- 唯一索引
- 普通索引
- 联合索引

---

## Q8：为什么 InnoDB 主要使用 B+Tree 作为索引结构？

**答：**

原因主要有以下几点：

### 1）更适合磁盘存储
数据库数据量通常很大，不可能全部在内存里。  
B+Tree 是多路平衡树，树的高度低，每次查找需要访问的磁盘页更少。

### 2）查询稳定
B+Tree 的数据都在叶子节点，所有查询最终都要落到叶子节点，所以查询路径稳定。

### 3）适合范围查询
B+Tree 叶子节点之间通过链表相连，范围查询和排序非常高效。

### 4）扇出高
B+Tree 非叶子节点只存索引值和指针，不存整行数据，所以一个页里能放更多索引项，树更矮。

### 对比其他结构
#### 对比二叉搜索树
- 高度容易过高
- 磁盘 IO 次数多

#### 对比红黑树
- 尽管平衡，但分支少，树依然高

#### 对比 Hash
- 只适合等值查询
- 不支持范围查询、排序、最左匹配

### 一句话总结
**数据库索引最重要的是减少磁盘访问，而 B+Tree 在这方面最优。**

---

## Q9：什么是聚簇索引？

**答：**

在 InnoDB 中，**数据行本身存储在主键索引的叶子节点上**，这种索引叫聚簇索引。

也就是说：
- 主键索引叶子节点 = 数据页
- 查主键时，不需要再查一次别的地方，直接拿到整行数据

### 特点
- 一个表只能有一个聚簇索引
- 通常是主键索引
- 如果没有显式主键，InnoDB 会选择一个唯一非空索引；再没有的话会生成隐藏 row_id

---

## Q10：什么是二级索引？

**答：**

除聚簇索引之外的普通索引，通常都是二级索引。

在 InnoDB 中：
- 二级索引叶子节点存储的是：**索引列值 + 主键值**
- 它不直接存整行数据

因此，通过二级索引查到目标后，通常还要再根据主键去聚簇索引查整行数据，这个过程叫**回表**。

---

## Q11：什么是回表？

**答：**

当查询先走二级索引定位到主键，再根据主键去聚簇索引取完整行数据时，这个过程就叫回表。

### 举例
假设有索引 `(name)`：

```sql
select * from user where name = 'Bob';
```

如果 `name` 是普通索引：
1. 先在 `name` 索引树找到主键 id
2. 再去主键索引树找到整行数据

这就是回表。

### 回表的问题
- 多一次索引查找
- 查询量大时会明显增加成本

---

## Q12：什么是覆盖索引？

**答：**

如果一个查询需要的字段，全部都能从索引中直接拿到，就不需要回表，这种情况叫**覆盖索引**。

### 举例
有联合索引 `(name, age)`：

```sql
select name, age from user where name = 'Bob';
```

因为查询字段都在索引里，所以不需要回表。

### 好处
- 减少一次聚簇索引访问
- 提高查询性能

### 面试加分说法
覆盖索引是减少回表、提升 SQL 性能的高频优化手段。

---

## Q13：联合索引为什么要遵守最左前缀匹配？

**答：**

联合索引底层是按照字段顺序排序构建的。  
例如索引 `(a, b, c)`，本质排序规则是：
1. 先按 a 排
2. a 相同时按 b 排
3. a、b 相同时按 c 排

所以它可以支持：
- `a`
- `a, b`
- `a, b, c`

但不能直接高效支持：
- `b`
- `c`
- `b, c`

因为缺少最左边字段，索引的有序性就无法直接利用。

### 一句话总结
**联合索引从左往右生效，不能跳过最左列单独利用后面的列。**

---

## Q14：索引失效有哪些常见场景？

**答：**

高频场景如下：

### 1）对索引列使用函数或表达式
```sql
where year(create_time) = 2026
```
因为数据库要先计算函数结果，无法直接利用原始索引顺序。

### 2）隐式类型转换
例如：
- 字段是字符串
- 查询条件传了数字

MySQL 可能会发生类型转换，导致索引失效。

### 3）联合索引不满足最左匹配
索引 `(a,b,c)`，但只按 `b=xx` 查，一般不能高效走索引。

### 4）like 以 `%` 开头
```sql
where name like '%abc'
```
前缀不固定，无法利用 B+Tree 的有序性。

### 5）or 条件一边走索引、一边不走索引
优化器可能选择全表扫描。

### 6）查询条件选择性太差
即使有索引，优化器也可能觉得全表扫描更划算。

---

## Q15：索引是不是越多越好？

**答：**

不是。

索引的优点：
- 提高查询效率
- 优化排序、分组、范围查询

索引的代价：
- 占用磁盘空间
- 占用内存
- 写入时要维护索引
- 会影响 insert、update、delete 性能

### 工程视角
索引设计不是越多越好，而是：
- 为高频查询设计
- 为核心路径设计
- 为 where / order by / group by 设计

---

## Q16：主键为什么通常建议自增且尽量短？

**答：**

### 1）主键是聚簇索引
聚簇索引叶子节点存整行数据，主键的顺序会影响数据页组织方式。

### 2）自增主键写入更顺序
新数据通常追加到 B+Tree 末尾，减少页分裂，写入更高效。

### 3）主键越短，二级索引越小
因为二级索引叶子节点保存主键值，主键太长会导致所有二级索引膨胀。

### 不建议使用 UUID 作为主键的原因
- 无序
- 插入离散
- 容易页分裂
- 占空间更大

---

# 四、事务篇

## Q17：什么是事务？

**答：**

事务是一组操作的集合，这组操作要么全部成功，要么全部失败，不允许只完成一部分。

### 举例
转账场景：
1. A 扣 100
2. B 加 100

这两个操作必须一起成功，否则数据就不一致。

---

## Q18：事务的 ACID 分别是什么？

**答：**

### A：Atomicity（原子性）
事务中的操作要么全部成功，要么全部失败。

### C：Consistency（一致性）
事务执行前后，数据库都必须处于一致状态。

### I：Isolation（隔离性）
多个事务并发执行时，彼此之间互不干扰。

### D：Durability（持久性）
事务一旦提交，结果就应该被永久保存，即使数据库宕机也不能丢。

---

## Q19：ACID 是靠什么实现的？

**答：**

### 原子性
主要依赖 **undo log**
- 事务失败时可回滚

### 持久性
主要依赖 **redo log**
- 宕机后可恢复已提交事务

### 隔离性
主要依赖：
- 锁机制
- MVCC

### 一致性
一致性不是单独由一个组件保证的，而是原子性、隔离性、持久性共同作用的结果。

---

# 五、隔离级别与并发问题

## Q20：事务并发会带来哪些问题？

**答：**

主要有三个经典问题：

### 1）脏读
读到了其他事务尚未提交的数据。

### 2）不可重复读
同一个事务里，两次读取同一条记录，结果不同。

### 3）幻读
同一个事务里，两次按条件查询，返回的记录数量不同，好像出现了“幻觉”。

---

## Q21：MySQL 的四种隔离级别是什么？

**答：**

### 1）Read Uncommitted（读未提交）
- 可能发生脏读、不可重复读、幻读

### 2）Read Committed（读已提交）
- 避免脏读
- 仍可能发生不可重复读、幻读

### 3）Repeatable Read（可重复读）
- 避免脏读、不可重复读
- InnoDB 下通过 MVCC 和锁，基本解决幻读问题

### 4）Serializable（串行化）
- 最严格
- 并发性能最差

### MySQL 默认隔离级别
- InnoDB 默认是 **Repeatable Read**

---

## Q22：不可重复读和幻读的区别是什么？

**答：**

### 不可重复读
关注的是：**同一条记录被修改了**

例如：
- 第一次查 id=1，name=Alice
- 第二次查 id=1，name=Bob

### 幻读
关注的是：**满足条件的记录集合发生变化**

例如：
- 第一次查年龄 > 20，有 10 条
- 第二次查年龄 > 20，有 11 条

### 一句话区分
- 不可重复读：同一行内容变了
- 幻读：满足条件的记录数量变了

---

# 六、MVCC 篇

## Q23：什么是 MVCC？

**答：**

MVCC，全称 **Multi-Version Concurrency Control**，即多版本并发控制。

它的核心思想是：
- 一份数据可以有多个版本
- 读操作读历史快照
- 写操作创建新版本

这样做的好处是：
- 普通读不加锁
- 读写不冲突
- 提升并发性能

---

## Q24：MVCC 依赖哪些核心机制？

**答：**

主要依赖三部分：

### 1）隐藏字段
InnoDB 每行记录会隐含一些事务相关字段，例如：
- 创建该版本的事务 id
- 删除该版本的事务 id（或回滚指针相关信息）

### 2）undo log
保存老版本数据，用于回滚，也为快照读提供历史版本链。

### 3）Read View
决定当前事务可以看到哪些版本，不能看到哪些版本。

---

## Q25：Read Committed 和 Repeatable Read 下的 MVCC 有什么区别？

**答：**

### Read Committed
每次执行一致性读，通常都会生成新的 Read View。  
所以：
- 事务内前后两次查询，可能看到不同版本

### Repeatable Read
事务第一次一致性读时生成 Read View，后续通常复用这个 Read View。  
所以：
- 同一事务里多次查询，通常看到的是同一个快照

### 本质区别
**Read View 的创建时机不同。**

---

## Q26：什么是快照读？什么是当前读？

**答：**

### 快照读
读取的是数据的历史可见版本，通常不加锁。

典型语句：
```sql
select * from user where id = 1;
```

### 当前读
读取的是最新版本数据，并且通常会加锁。

典型语句：
```sql
select * from user where id = 1 for update;
select * from user where id = 1 lock in share mode;
update ...
delete ...
insert ...
```

### 一句话总结
- 普通 `select` 多数是快照读
- 加锁读 / 更新 / 删除 属于当前读

---

## Q27：RR 隔离级别是怎么解决幻读的？

**答：**

要分情况说：

### 1）快照读场景
通过 MVCC，事务在自己的 Read View 下看到固定快照，因此不会“感知到”其他事务新插入的记录。

### 2）当前读场景
通过 **Next-Key Lock** 防止其他事务在查询范围内插入新记录。

### 更准确的说法
MySQL InnoDB 在 RR 下并不是靠单一机制解决幻读，而是：
- **快照读靠 MVCC**
- **当前读靠间隙锁 / Next-Key Lock**

---

# 七、锁篇

## Q28：MySQL 里常见的锁有哪些？

**答：**

### 按粒度分
- 全局锁
- 表锁
- 行锁

### 按模式分
- 共享锁（S 锁）
- 排他锁（X 锁）

### InnoDB 行锁细分
- 记录锁（Record Lock）
- 间隙锁（Gap Lock）
- Next-Key Lock

---

## Q29：什么是共享锁和排他锁？

**答：**

### 共享锁（S）
- 也叫读锁
- 多个事务可以同时持有共享锁
- 但不能与排他锁共存

### 排他锁（X）
- 也叫写锁
- 一个事务持有排他锁后，其他事务不能再对该数据加共享锁或排他锁

---

## Q30：什么是记录锁、间隙锁、Next-Key Lock？

**答：**

### 1）记录锁
锁住一条已经存在的索引记录。

### 2）间隙锁
锁住索引记录之间的间隙，不锁具体记录，主要用于防止插入。

### 3）Next-Key Lock
记录锁 + 间隙锁，锁住一个范围。

### 作用
用于在 RR 隔离级别下，防止当前读场景发生幻读。

---

## Q31：为什么 update / delete 没有索引会很危险？

**答：**

因为 InnoDB 的行锁是加在**索引记录**上的。  
如果 update / delete 条件没有命中索引，就可能：
- 扫描大量记录
- 锁住大量索引项
- 甚至表现得像“锁全表”一样

### 后果
- 并发下降
- 容易阻塞
- 容易死锁
- 慢 SQL 更严重

### 面试加分点
**锁范围的大小，往往和是否走索引强相关。**

---

## Q32：什么是死锁？

**答：**

死锁是指多个事务相互等待对方持有的资源，形成循环等待，导致所有事务都无法继续执行。

### 举例
事务 A 锁住记录 1，等待记录 2  
事务 B 锁住记录 2，等待记录 1  
形成死锁。

---

## Q33：如何避免死锁？

**答：**

常见思路：

### 1）固定加锁顺序
例如都先锁小 id，再锁大 id。

### 2）让 SQL 尽量命中索引
减少锁范围。

### 3）缩短事务时间
不要在事务里做太多无关逻辑。

### 4）避免大事务
事务越大，持锁时间越长，越容易死锁。

### 5）减少不必要的当前读
比如少用无意义的 `for update`。

---

## Q34：MySQL 发生死锁后怎么办？

**答：**

InnoDB 会做死锁检测，并主动回滚其中一个事务，让另一个事务继续执行。

### 业务层怎么做
- 捕获死锁异常
- 适当重试
- 从设计上减少死锁概率

---

# 八、日志篇

## Q35：MySQL 中常见日志有哪些？

**答：**

MySQL 面试里最常问的三种日志：

- undo log
- redo log
- binlog

除此之外还有：
- 慢查询日志
- error log
- relay log（主从复制中间日志）
- general log（一般不常用）

---

## Q36：undo log 是什么？作用是什么？

**答：**

undo log 是回滚日志。

### 作用 1：事务回滚
事务执行失败或手动回滚时，可以利用 undo log 恢复到事务前状态。

### 作用 2：MVCC
undo log 保存历史版本，供快照读回溯旧版本数据。

### 一句话总结
**undo log 既服务于回滚，也服务于 MVCC。**

---

## Q37：redo log 是什么？为什么需要 redo log？

**答：**

redo log 是 InnoDB 的重做日志，用于保证事务提交后的数据持久性。

### 为什么需要它？
MySQL 修改数据时，通常不是立刻把数据页刷盘，而是先修改内存中的 Buffer Pool。  
如果此时宕机，内存数据会丢失。

redo log 的作用就是：
- 先把“修改操作”以日志方式持久化
- 崩溃恢复时，根据 redo log 重新应用修改

### 这样带来的好处
- 实现 crash-safe
- 顺序写日志，比随机刷数据页更高效

---

## Q38：binlog 是什么？作用是什么？

**答：**

binlog 是 MySQL Server 层的二进制日志。

### 主要作用
1. 主从复制
2. 数据恢复
3. 审计和变更追踪

### 特点
- 记录逻辑层的数据变更
- 不只 InnoDB 用，MySQL Server 层统一管理

---

## Q39：redo log 和 binlog 的区别是什么？

**答：**

### redo log
- InnoDB 引擎层日志
- 偏物理日志
- 主要用于崩溃恢复
- 循环写

### binlog
- MySQL Server 层日志
- 偏逻辑日志
- 主要用于主从复制和归档
- 追加写

### 一句话总结
- redo log 保证“库挂了也能恢复已提交事务”
- binlog 保证“能复制、能归档、能回放变更”

---

## Q40：为什么有了 binlog 还需要 redo log？

**答：**

因为两者职责不同：

### binlog 的重点
- 记录逻辑变更
- 用于复制和归档

### redo log 的重点
- 保证 InnoDB 崩溃恢复
- 让事务提交具备 crash-safe 能力

因此两者不能互相替代。

---

## Q41：什么是两阶段提交？为什么需要两阶段提交？

**答：**

两阶段提交是为了保证 **redo log 和 binlog 的一致性**。

### 如果没有两阶段提交，可能出现的问题
#### 情况 1：redo log 成功，binlog 失败
- 主库本地恢复后数据存在
- 但 binlog 没记录
- 从库复制不到这次更新
- 主从不一致

#### 情况 2：binlog 成功，redo log 失败
- binlog 记录了变更
- 从库同步到了
- 但主库宕机后可能恢复不出来
- 主从仍不一致

### 两阶段提交简化理解
1. redo log prepare
2. 写 binlog
3. redo log commit

### 目的
**确保 redo log 和 binlog 要么都成功，要么都失败。**

---

# 九、Buffer Pool 与刷盘机制

## Q42：什么是 Buffer Pool？

**答：**

Buffer Pool 是 InnoDB 在内存中维护的缓存区域，用来缓存：
- 数据页
- 索引页
- 部分辅助信息

### 作用
- 减少磁盘 IO
- 提高读写性能

### 为什么重要
数据库访问磁盘很慢，而内存很快。  
Buffer Pool 能把热点数据尽量留在内存中。

---

## Q43：为什么修改数据不是立刻刷盘？

**答：**

因为直接刷盘成本很高，尤其是随机写。  
MySQL 通常做法是：
1. 先修改内存中的页
2. 记录 redo log
3. 后续再异步刷盘

### 好处
- 写入更快
- 吞吐更高
- 更适合高并发场景

---

# 十、SQL 执行流程

## Q44：一条 SQL 在 MySQL 中是怎么执行的？

**答：**

以查询语句为例，整体流程通常是：

### 1）连接器
- 建立连接
- 身份认证
- 权限校验

### 2）解析器
- 词法分析
- 语法分析
- 判断 SQL 是否符合语法

### 3）预处理/语义检查
- 检查表和字段是否存在
- 展开 `select *`

### 4）优化器
- 选择执行计划
- 决定走哪个索引
- 决定 join 顺序
- 决定排序方式等

### 5）执行器
- 调用存储引擎接口执行
- 返回结果

---

## Q45：为什么要看 EXPLAIN？

**答：**

因为 EXPLAIN 能帮助我们看到 SQL 的执行计划。

常看的信息包括：
- type：访问类型
- key：使用了哪个索引
- rows：预估扫描行数
- extra：额外信息，如 Using index、Using filesort、Using temporary

### EXPLAIN 的意义
不是看 SQL “写得对不对”，而是看数据库“打算怎么执行”。

---

## Q46：常见的 SQL 执行性能问题有哪些？

**答：**

- 全表扫描
- 回表过多
- 排序无法利用索引
- 临时表过大
- 深分页
- join 顺序不佳
- 索引失效
- 扫描行数远大于返回行数

---

# 十一、SQL 优化篇

## Q47：SQL 优化应该怎么做？

**答：**

推荐按照这个顺序回答：

### 1）先定位问题
- 慢查询日志
- 监控系统
- APM
- 业务报警

### 2）再分析执行计划
- EXPLAIN
- 看是否走索引
- 看扫描行数
- 看是否排序、临时表

### 3）再做优化
- 加索引 / 改索引
- 改写 SQL
- 减少回表
- 减少无效字段查询
- 调整表结构
- 分页优化

### 4）最后做架构优化
- 读写分离
- 缓存
- 分库分表
- 异步削峰

---

## Q48：深分页为什么慢？怎么优化？

**答：**

### 为什么慢？
例如：
```sql
select * from user order by id limit 100000, 20;
```

数据库通常要先扫描前 100000 条，再丢弃，然后取后 20 条。  
offset 越大，扫描和丢弃成本越高。

### 常见优化方式
#### 1）基于主键/游标分页
```sql
select * from user where id > last_id order by id limit 20;
```

#### 2）先通过覆盖索引取 id，再回表
减少大范围回表。

#### 3）业务上限制最大页码
避免用户翻到极深页。

---

## Q49：为什么不推荐 `select *`？

**答：**

原因：
- 会读取不需要的字段
- 增加网络传输开销
- 可能影响覆盖索引的使用
- 字段变多后风险更高

### 更好的方式
只查需要的列。

---

## Q50：count(*)、count(1)、count(列) 有什么区别？

**答：**

### count(*)
统计行数，不关心具体列是否为 null。

### count(1)
本质上也是统计行数。

### count(列)
只统计该列非 null 的行数。

### 面试中的重点
- 语义差别要搞清楚
- 不要机械背“哪个一定更快”
- 真实性能还要结合执行计划和存储引擎看

---

# 十二、主从复制与读写分离

## Q51：MySQL 主从复制是什么？

**答：**

主从复制是指：
- 主库负责写
- 从库通过复制主库的 binlog，同步数据变更
- 从库承接读请求或作为备份

### 作用
- 提高可用性
- 分摊读压力
- 支持备份与容灾

---

## Q52：主从复制的基本流程是什么？

**答：**

简化理解：

1. 主库写入数据并生成 binlog
2. 从库 IO 线程拉取主库 binlog，写入 relay log
3. 从库 SQL 线程读取 relay log 并重放执行

---

## Q53：什么是读写分离？

**答：**

读写分离是指：
- 写请求发到主库
- 读请求发到从库

### 目的
- 降低主库读压力
- 提高整体吞吐

---

## Q54：读写分离有什么问题？

**答：**

最经典的问题是：**主从延迟**

### 表现
刚写入主库的数据，立刻去从库读，可能读不到最新结果。

### 应对方式
- 强一致读走主库
- 写后短时间内读主库
- 接受最终一致
- 重要场景做读写路由控制

---

# 十三、分库分表篇

## Q55：为什么要分库分表？

**答：**

当单机数据库在以下方面遇到瓶颈时，通常要考虑分库分表：
- 数据量太大
- 并发太高
- 单表过大
- 单机 CPU、IO、连接数扛不住
- 业务扩展导致表越来越重

---

## Q56：分库分表有哪些方式？

**答：**

### 1）垂直拆分
按业务维度拆分。

例如：
- 用户库
- 订单库
- 支付库

### 2）水平拆分
同一类数据按规则打散到多个库或表中。

例如：
- 按 user_id % 16 分表

---

## Q57：分库分表会带来哪些问题？

**答：**

### 1）跨库 join 困难
原来一条 SQL 能搞定的，现在需要应用层聚合。

### 2）分布式事务复杂
跨库写入不再像单库事务那么简单。

### 3）分页、排序、统计复杂
需要跨分片汇总。

### 4）分片键选不好会导致热点
例如按时间分，可能热数据都打到一张表。

### 5）扩容迁移复杂
历史数据搬迁代价高。

### 一句话总结
**分库分表解决的是单机瓶颈，但会把数据库问题升级为系统复杂度问题。**

---

# 十四、Redis 基础篇

## Q58：Redis 为什么快？

**答：**

Redis 快主要有几个原因：

### 1）基于内存
内存访问远快于磁盘。

### 2）数据结构高效
Redis 提供了适合不同场景的数据结构。

### 3）核心命令执行模型简单
减少了线程切换和锁竞争开销。

### 4）I/O 多路复用
提高网络处理效率。

### 一句话总结
**Redis 快的根本原因是：内存 + 高效数据结构 + 低开销执行模型。**

---

## Q59：Redis 常见数据结构有哪些？适合什么场景？

**答：**

### 1）String
场景：
- 缓存对象
- 计数器
- 分布式锁
- token/session

### 2）Hash
场景：
- 存对象属性
- 用户信息、配置项

### 3）List
场景：
- 简单消息队列
- 按顺序存储元素

### 4）Set
场景：
- 去重
- 共同好友
- 标签集合

### 5）ZSet
场景：
- 排行榜
- 延迟队列
- 按分值排序的数据

---

## Q60：Redis 持久化有哪些方式？

**答：**

### 1）RDB
定期生成快照文件。

优点：
- 文件紧凑
- 恢复快
- 适合备份

缺点：
- 宕机时可能丢失最后一次快照之后的数据

### 2）AOF
记录写命令追加到日志。

优点：
- 数据更完整
- 可按策略控制刷盘频率

缺点：
- 文件更大
- 恢复可能更慢

### 实际使用
很多场景会同时使用 RDB + AOF。

---

# 十五、Redis 缓存问题

## Q61：什么是缓存穿透？

**答：**

请求的数据在 Redis 和 MySQL 中都不存在，导致每次请求都会打到数据库。

### 常见原因
- 恶意请求不存在的 key
- 业务参数异常

### 解决方案
- 缓存空值
- 布隆过滤器
- 参数校验

---

## Q62：什么是缓存击穿？

**答：**

某个热点 key 失效的瞬间，大量请求同时访问数据库，导致数据库压力暴涨。

### 解决方案
- 热点 key 不过期或逻辑过期
- 互斥锁 / singleflight
- 后台异步刷新

---

## Q63：什么是缓存雪崩？

**答：**

大量 key 在同一时间过期，导致大量请求一起打到数据库。

### 解决方案
- 过期时间加随机值
- 多级缓存
- 限流降级
- 预热缓存

---

## Q64：缓存和数据库如何保持一致？

**答：**

常见策略是 **Cache Aside Pattern**：

### 读流程
1. 先查缓存
2. 缓存没有再查数据库
3. 将结果写入缓存

### 写流程
1. 更新数据库
2. 删除缓存

### 为什么通常不是“先删缓存再更新数据库”？
因为可能出现并发下脏数据回写的问题。

### 为什么是“更新数据库后删除缓存”？
这是工程上更常见、风险更小的策略。

### 但它也不是绝对完美
高并发场景下仍可能有短暂不一致，因此可能配合：
- 延迟双删
- MQ 异步通知
- binlog 订阅回写缓存

---

# 十六、Redis 分布式锁

## Q65：Redis 分布式锁怎么实现？

**答：**

典型加锁命令：

```bash
SET lock_key unique_value NX PX 30000
```

### 各参数含义
- `NX`：只有 key 不存在时才能设置成功
- `PX 30000`：锁 30 秒后自动过期
- `unique_value`：每个客户端自己的唯一标识

### 本质
这条命令把：
- 加锁
- 防止重复加锁
- 自动过期
合并成一个原子操作。

---

## Q66：为什么不能只用 SETNX？

**答：**

因为如果只 `SETNX` 成功，但没有设置过期时间，一旦持锁线程挂了，锁就可能永远不释放，造成死锁。

### 正确思路
加锁和设置过期时间必须是原子操作。

---

## Q67：为什么释放锁时要校验 value？

**答：**

因为锁可能过期后被其他客户端重新获取。  
如果旧客户端这时候直接删除 key，就可能把新客户端的锁删掉。

### 所以要怎么做？
- 每个客户端加锁时写入唯一 value
- 解锁时先比较 value，再删除
- 一般用 Lua 脚本保证“比较 + 删除”的原子性

---

## Q68：Redis 分布式锁有哪些常见问题？

**答：**

### 1）业务没执行完，锁先过期
导致多个客户端同时进入临界区。

### 2）误删他人锁
释放锁不校验 value。

### 3）主从切换导致锁丢失
主节点加锁成功但还没同步到从节点，主节点宕机，切换后锁信息丢失。

### 4）时钟 / 网络 / 超时等问题
在复杂分布式环境下，锁的绝对安全性很难保证。

### 面试中更稳妥的说法
Redis 分布式锁适合做**业务层互斥控制**，但要根据一致性要求评估是否足够。

---

# 十七、Redis 高可用

## Q69：Redis 主从复制是什么？

**答：**

Redis 主从复制是指：
- 主节点负责写
- 从节点同步主节点数据
- 从节点可用于读扩展和故障恢复

---

## Q70：Redis Sentinel（哨兵）是做什么的？

**答：**

哨兵用于 Redis 主从架构下的高可用管理。

主要功能：
- 监控主从节点是否正常
- 自动故障转移
- 选举新的主节点
- 通知客户端新的主节点地址

### 一句话总结
**哨兵解决的是 Redis 主节点挂了以后怎么办。**

---

## Q71：Redis Cluster 是什么？

**答：**

Redis Cluster 是 Redis 的分布式集群方案，用于解决：
- 单机内存不够
- 单机吞吐不够
- 单点故障问题

### 特点
- 数据分片存储
- 高可用
- 每个节点只存部分数据

---

# 十八、BigKey / HotKey

## Q72：什么是 BigKey？

**答：**

BigKey 指单个 key 对应的数据量过大。

### 举例
- 一个 String 特别大
- 一个 Hash 有几十万 field
- 一个 ZSet 有几百万 member

### 危害
- 删除慢
- 迁移慢
- 读取慢
- 容易阻塞 Redis
- 主从同步压力大

### 解决思路
- 拆分 key
- 分片
- 避免单 key 聚集过多数据
- 大 key 删除用异步方式

---

## Q73：什么是 HotKey？

**答：**

HotKey 指访问量极高、请求非常集中的 key。

### 危害
- 单点热点
- Redis 某个节点压力过大
- 网络带宽和 CPU 被打满

### 解决思路
- 本地缓存
- 多副本分担读
- 热点探测与降级
- 业务限流

---

# 十九、数据库设计与实践题

## Q74：数据库表设计时要注意什么？

**答：**

常见关注点：

### 1）主键设计
- 尽量短
- 尽量稳定
- 通常自增或趋势递增

### 2）字段类型
- 能用 int 不乱用 bigint
- 能定长时考虑定长
- 不要无脑 varchar(255)

### 3）是否允许 null
- 结合业务语义
- 避免滥用 null

### 4）索引设计
- 根据查询模式建索引
- 避免冗余索引

### 5）冗余设计
- 对高频查询允许适度反范式
- 但要控制一致性复杂度

---

## Q75：为什么不建议在业务高峰时随便加索引？

**答：**

因为加索引本身也可能是重操作：
- 占用 IO
- 占用 CPU
- 影响写入
- 大表上执行时间长

### 工程上要考虑
- 是否在线 DDL
- 是否会锁表
- 是否分批执行
- 是否需要灰度

---

## Q76：什么时候应该考虑不用数据库硬扛，而是引入缓存或 MQ？

**答：**

当出现以下情况时：
- 读远高于写
- 热点访问明显
- 峰值流量冲击数据库
- 下游处理可以异步化
- 某些统计类数据不要求强一致

这时可以通过：
- Redis 缓存
- MQ 削峰填谷
- 异步落库
- 预计算

来减轻数据库压力。

---

# 二十、面试高频综合题

## Q77：MySQL 查询慢，一般怎么排查？

**答：**

推荐这样回答：

### 第一步：确认现象
- 是偶发还是持续
- 是单条 SQL 慢还是整体变慢
- 是读慢还是写慢

### 第二步：查监控和日志
- 慢查询日志
- CPU、IO、连接数
- 锁等待
- QPS/TPS
- Buffer Pool 命中率

### 第三步：看 SQL 执行计划
- 是否走索引
- 扫描行数是否过多
- 是否回表严重
- 是否 filesort / temporary

### 第四步：分析数据分布和业务特征
- 查询条件选择性是否太差
- 是否深分页
- 是否热点数据
- 是否大事务 / 锁冲突

### 第五步：制定优化方案
- 改索引
- 改 SQL
- 改分页方式
- 做缓存
- 做读写分离
- 做分库分表

---

## Q78：为什么说“查询靠索引，正确性靠事务，并发靠 MVCC 和锁，恢复靠日志”？

**答：**

这是数据库知识体系的主线：

### 查询靠索引
索引决定查得快不快。

### 正确性靠事务
事务保证操作不会只做一半。

### 并发靠 MVCC 和锁
高并发下，既要保证效率，也要保证一致性。

### 恢复靠日志
宕机后能不能恢复数据，取决于日志体系。

### 扩展靠架构
当单库不够时，再往上做：
- 主从复制
- 读写分离
- 分库分表
- 缓存和异步化

---

## Q79：如果让你用一句话总结数据库面试主线，你会怎么说？

**答：**

可以这样回答：

**数据库面试的核心主线就是：索引解决查询效率，事务解决数据正确性，MVCC 和锁解决并发控制，日志解决崩溃恢复，Redis 和架构手段解决高并发扩展问题。**

---

# 二十一、背诵优先级建议

如果时间有限，优先掌握下面这些题：

## P0：必须拿下
- 什么是索引，为什么用 B+Tree
- 聚簇索引、二级索引、回表、覆盖索引
- 联合索引和最左匹配
- 索引失效场景
- 事务和 ACID
- 脏读、不可重复读、幻读
- MVCC 是什么
- 记录锁、间隙锁、Next-Key Lock
- undo log、redo log、binlog
- 两阶段提交
- Redis 为什么快
- 缓存穿透、击穿、雪崩
- Redis 分布式锁

## P1：高频追问
- Buffer Pool
- SQL 执行流程
- EXPLAIN 怎么看
- 深分页优化
- 主从复制
- 读写分离和主从延迟
- 分库分表问题
- BigKey、HotKey
- Redis 哨兵和集群

## P2：进阶加分
- 为什么主键建议自增
- 为什么 update 没索引危险
- 为什么 count(列) 与 count(*) 不同
- 为什么缓存一致性做不到绝对无损
- 为什么分库分表是“用复杂度换扩展性”

---

# 二十二、面试输出模板（适合现场表达）

## 模板 1：回答原理题
可以按这个顺序：
1. 先下定义
2. 讲核心原理
3. 讲为什么这样设计
4. 讲优缺点
5. 讲工程上的注意点

例如回答“为什么用 B+Tree”：
- 先说 B+Tree 是 InnoDB 常用索引结构
- 再说它多路平衡、树高低、适合磁盘
- 再说叶子节点链表适合范围查询
- 再对比 Hash、红黑树
- 最后说它适合数据库场景

## 模板 2：回答排查题
1. 先确认问题范围
2. 再看日志和监控
3. 再看执行计划
4. 再分析数据和业务
5. 最后给出优化方案

## 模板 3：回答架构题
1. 先说单机瓶颈在哪里
2. 再说为什么引入某个方案
3. 再说收益
4. 再说带来的新问题
5. 最后说如何权衡

---

# 二十三、最后总结

数据库面试不要把它看成一堆零散八股，而要形成完整主线：

- **存储层**：表、页、索引、B+Tree
- **查询层**：执行计划、回表、覆盖索引、慢 SQL
- **事务层**：ACID、隔离级别、MVCC、锁
- **恢复层**：undo log、redo log、binlog
- **缓存层**：Redis、缓存异常、分布式锁
- **架构层**：主从复制、读写分离、分库分表、削峰限流

如果你能把这六层串起来，数据库面试基本就不是“背题”，而是“讲系统”。

---

> 这份文档适合继续扩展为：
> 1. 数据库面试 100 问  
> 2. MySQL 专项面试题  
> 3. Redis 专项面试题  
> 4. 高并发数据库场景题  
> 5. 面试背诵速记版


