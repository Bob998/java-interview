# Kafka 面试问答（全面细致版）

> 覆盖：基础概念 / 架构设计 / Topic 与 Partition / 副本机制 / ISR / 消费模型 / 顺序性 / 可靠性 / 重复消费 / 消息积压 / Zookeeper 与 KRaft / 性能优化 / 重试与死信队列 / 高频综合题  
> 参考整理方向：小林coding、JavaGuide  
> 风格：面试问答 + 原理拆解 + 高频追问 + 实战表达

---

# 一、Kafka 基础认知

## Q1：什么是 Kafka？

Kafka 是一个分布式消息系统，采用发布-订阅模型。生产者（Producer）把消息发送到 Topic，消费者（Consumer）通过订阅 Topic 来消费消息。  
如果从系统定位上看，Kafka 不只是一个传统意义上的消息队列，它更接近一个**分布式日志系统 / 流式数据平台**。

你可以把 Kafka 理解成：

- 对外表现为消息队列
- 对内本质是分布式追加日志
- 在大数据和实时计算场景里，又可以作为流式数据总线

所以面试时比较稳妥的说法是：

> Kafka 是一个高吞吐、高可用、可持久化的分布式消息系统，本质上也是一个分布式日志系统。

---

## Q2：Kafka 的核心组件有哪些？

Kafka 面试里至少要知道以下几个基础概念：

### 1）Producer
生产者，负责发送消息。

### 2）Consumer
消费者，负责拉取和处理消息。

### 3）Broker
Kafka 服务节点。一个 Kafka 集群由多个 Broker 组成。

### 4）Topic
消息主题，是逻辑上的消息分类。

### 5）Partition
分区，是 Topic 的物理划分单位。消息真正存储在 Partition 中。

### 6）Consumer Group
消费者组。多个消费者组成一个组，组内共同消费一个 Topic，实现负载均衡。

一句话总结：

> Topic 是逻辑概念，Partition 是物理概念，Broker 是承载节点，Producer/Consumer 是收发两端，Consumer Group 是并行消费的组织方式。

---

## Q3：Kafka 为什么常被说成高吞吐消息队列？

Kafka 的高吞吐，主要来自以下几方面：

- Topic 可以拆成多个 Partition，并行读写
- 多个 Partition 可以分布到不同 Broker 上，支持水平扩展
- Kafka 采用顺序追加写日志的方式存储消息
- Kafka 利用 Page Cache 和零拷贝提升磁盘和网络传输效率

所以 Kafka 和很多传统 MQ 不同，它不是特别强调复杂的路由，而是更强调：

- 吞吐
- 持久化
- 横向扩展
- 实时流式处理能力

---

# 二、Topic 与 Partition

## Q4：什么是 Topic？

Topic 是 Kafka 中消息的逻辑分类。  
生产者把消息发送到某个 Topic，消费者订阅某个 Topic 来消费消息。

Topic 本身只是逻辑概念，真正决定消息存储和并发能力的是 Partition。

---

## Q5：什么是 Partition？

Partition 是 Topic 的物理分区。  
一个 Topic 可以包含多个 Partition，每个 Partition 都是一条有序的追加日志。

Kafka 中消息最终不是直接存到 Topic，而是存到 Topic 的某个 Partition 里。

面试里可以这样说：

> Topic 用于逻辑分类，Partition 用于物理存储与并发处理。

---

## Q6：Kafka 为什么要分区？

Kafka 分区的核心目的是两个：

### 1）提高吞吐
多个 Partition 可以并行读写，提升整体处理能力。

### 2）支持水平扩展
不同 Partition 可以分布到不同 Broker 上，随着集群扩容而扩展。

如果一个 Topic 只有一个 Partition，那么这个 Topic 的读写能力和消费能力都很有限；如果一个 Topic 有多个 Partition，就可以利用多个 Broker 和多个消费者实例并行处理。

---

## Q7：Kafka 中分区和传统 MQ 的“队列”是什么关系？

可以把 Kafka 的 Partition 近似理解成传统 MQ 里的队列。  
因为：

- 消息是按 Partition 存储的
- 消费时也是按 Partition 分配的
- 顺序性也是 Partition 级别保证的

所以 Kafka 的并发与顺序问题，本质上都是 Partition 问题。

---

## Q8：为什么说分区数决定了消费并发上限？

在同一个 Consumer Group 中：

- 一个 Partition 在同一时刻只能被一个消费者消费
- 一个消费者可以同时消费多个 Partition

所以如果一个 Topic 有 8 个 Partition，那么同一个消费组最多只能有 8 个消费者实例真正并行消费；第 9 个消费者即使加入组，也分不到 Partition。

因此：

> 分区数决定了同组消费并发上限。

---

## Q9：如果消费线程数超过分区数，会怎么样？

如果线程数或消费者实例数超过分区数，多出来的线程或实例不会分配到 Partition，相当于空转，造成资源浪费。

因此通常的经验是：

- 想提高消费并发，先看分区数够不够
- 消费线程数不要远大于分区数
- 分区数是并发上限，不是线程数越多越好

---

# 三、Kafka 副本机制与高可用

## Q10：Kafka 为什么需要副本机制？

Kafka 的消息是持久化在磁盘上的，如果某个 Broker 宕机，而对应分区只有一份数据，那么这个分区的数据就不可用了。

因此 Kafka 为 Partition 引入副本机制，目的主要是：

- 提高数据安全性
- 提高系统高可用性
- 在 Broker 故障时快速切换服务

---

## Q11：Kafka 的副本角色有哪些？

每个 Partition 通常会有多个副本，分为两种角色：

### 1）Leader 副本
负责处理生产者写入和消费者读取请求。

### 2）Follower 副本
不直接处理客户端请求，而是从 Leader 拉取消息进行同步。

对外服务只面向 Leader，这样能保证单分区内的顺序和一致性。

---

## Q12：Producer 和 Consumer 为什么只和 Leader 交互？

这样设计是为了简化一致性模型。

如果 Producer 同时往多个副本写，或者 Consumer 从不同副本读：

- 顺序会更难保证
- 一致性判断更复杂
- 客户端逻辑也会更重

所以 Kafka 采用了更简单直接的模式：

> 所有读写都走 Leader，Follower 负责同步。

---

## Q13：Kafka 的多分区和多副本分别解决什么问题？

这是一个很适合面试直接回答的题。

### 多分区解决：
- 并发能力
- 负载均衡
- 横向扩展

### 多副本解决：
- 高可用
- 数据安全
- 容灾切换

一句话总结：

> 分区管性能和扩展，副本管可靠性和高可用。

---

## Q14：Leader 宕机后怎么办？

当 Leader 所在 Broker 宕机时，Kafka 会从符合条件的 Follower 副本中选出新的 Leader 来接替工作。

这就是 Kafka 的故障转移能力来源。

不过这里的关键不只是“选一个副本”，而是：

> 必须从同步状态足够好的副本中选新 Leader，否则可能造成数据丢失。

这就引出了 ISR 机制。

---

# 四、ISR、acks 与消息可靠性

## Q15：什么是 ISR？

ISR 的全称是 **In-Sync Replicas**，表示“与 Leader 保持同步的副本集合”。

不是所有 Follower 都一定有资格参与选主，只有同步状态足够好的副本，才会进入 ISR。

所以你可以把 ISR 理解成：

> 当前“靠谱”的副本集合。

---

## Q16：为什么 Kafka 不从所有副本里选主，而只从 ISR 里选？

因为如果一个副本严重落后于 Leader，它虽然也是副本，但它的数据可能不完整。  
如果从这种落后副本里选新 Leader，就有可能丢掉已经写入旧 Leader 但还没同步过去的消息。

因此 Kafka 只从 ISR 中选主，核心目标是：

- 降低数据丢失风险
- 保证故障切换后数据尽可能完整

---

## Q17：什么是 acks？

acks 是生产者发送消息时的确认机制，决定了生产者在什么条件下认为“消息发送成功”。

常见取值有：

### 1）acks=0
生产者发出去就不等响应。性能最高，但最不可靠。

### 2）acks=1
Leader 收到消息就返回成功，不等 Follower 同步。

### 3）acks=all（或 -1）
只有 ISR 中所有副本都确认收到消息，才返回成功。可靠性最高。

---

## Q18：为什么 acks=all 更可靠？

因为它要求 ISR 中所有副本都完成同步后，生产者才认为发送成功。  
这样即使 Leader 之后马上故障，其他同步好的副本里也已经有这条消息，数据丢失风险更低。

所以在面试里，如果被问“如何提高 Kafka 的可靠性”，`acks=all` 是高频标准答案。

---

## Q19：`replication.factor` 和 `min.insync.replicas` 分别是什么？

### replication.factor
表示每个 Partition 的副本数。

例如设置为 3，表示一个分区会有 3 份副本。

### min.insync.replicas
表示在 acks=all 的前提下，至少要有多少个 ISR 副本确认成功，才算写入成功。

这是可靠性里的关键配置项。

---

## Q20：为什么生产环境常建议：
- `acks=all`
- `replication.factor >= 3`
- `min.insync.replicas > 1`

因为这样可以兼顾可靠性和可用性：

- `acks=all`：生产端等待 ISR 副本确认
- `replication.factor >= 3`：至少 3 副本，容错能力更强
- `min.insync.replicas > 1`：至少两个副本写成功才返回，避免只写 Leader 就算成功

此外，通常还会要求：

- `replication.factor > min.insync.replicas`
- `unclean.leader.election.enable = false`

这样能进一步降低脏选主和消息丢失风险。

---

# 五、Zookeeper 与 KRaft

## Q21：Kafka 为什么早期依赖 Zookeeper？

早期 Kafka 使用 Zookeeper 主要来做：

- 集群元数据管理
- Broker 注册
- Controller 选举
- Topic / Partition 元数据协调

也就是说，Kafka 本身负责消息读写，很多“集群协调和元数据管理”工作交给了 Zookeeper。

---

## Q22：Kafka 一定要依赖 Zookeeper 吗？

不一定。  
Kafka 2.8 之后引入了 **KRaft（Kafka Raft）** 模式，开始摆脱对 Zookeeper 的依赖。

在 KRaft 模式下，Kafka 自己使用 Raft 协议来管理元数据和控制器选举，不再需要单独部署 Zookeeper。

---

## Q23：KRaft 的意义是什么？

KRaft 的核心意义是：

- 简化部署架构
- 降低维护复杂度
- 让 Kafka 集群更“自洽”
- 不再依赖外部的 Zookeeper 集群

面试里你可以这么说：

> KRaft 的价值在于让 Kafka 自己完成元数据共识和管理，减少外部依赖。

---

# 六、Kafka 顺序性

## Q24：Kafka 能保证消息有序吗？

能，但只能保证**分区内有序**，不能天然保证 Topic 级别全局有序。

原因很简单：

- 同一个 Partition 是追加日志，天然有顺序
- 不同 Partition 之间是并行处理的，没有统一全局顺序

所以面试标准答法是：

> Kafka 只能保证单 Partition 内顺序，不能保证多 Partition 的全局顺序。

---

## Q25：Kafka 为什么只能保证分区内顺序？

因为一个 Topic 往往有多个 Partition，多个分区的消息会并行写入、并行消费。  
这些分区彼此独立，各自维护自己的 offset 和日志流，因此不存在天然统一的全局时序。

Kafka 的设计目标是吞吐和扩展，而不是全局强顺序。

---

## Q26：如何保证业务消息有序？

需要从生产端和消费端两侧同时保证。

### 生产端
让需要保持顺序的消息进入同一个 Partition。

常见做法有：

- 指定相同的 key
- 指定固定 partition
- 自定义分区器

### 消费端
同一 Partition 尽量串行处理，不要对单分区消息做无序并发处理。

完整表述是：

> 同一业务键路由到同一 Partition，再对该 Partition 串行消费。

---

## Q27：为什么相同 key 可以保证进入同一个 Partition？

因为默认分区策略通常会基于 key 做哈希计算。  
同样的 key，在分区数不变的情况下，通常会路由到同一个 Partition。

所以在订单、账户、用户状态流转等场景里，常见做法就是：

- 以订单号 / 用户 ID / 账户 ID 作为 key
- 让同一实体的消息落在同一个 Partition 上

---

## Q28：如果业务要求全局有序怎么办？

Kafka 原生不擅长全局有序。常见方案有两个：

### 1）单 Partition
让整个 Topic 只有一个 Partition。这样天然全局有序，但吞吐和并发能力会明显下降。

### 2）业务层排序补偿
在消息体中附加序号、版本号、时间戳等信息，消费后在业务侧做排序、校验、幂等和乱序修复。

所以全局有序往往是一个“吞吐换顺序”或“复杂度换顺序”的问题。

---

# 七、Producer 发送与可靠投递

## Q29：Producer 发送消息后就一定成功了吗？

不一定。  
生产者调用 `send()` 只是把消息交给客户端发送流程，并不代表 Broker 一定已经成功接收并持久化。

消息可能在以下环节失败：

- 网络抖动
- Broker 不可用
- Leader 切换
- 超时
- ISR 不满足条件

因此生产端一定要关注“发送结果”。

---

## Q30：Producer 如何判断消息发送是否成功？

常见方式有两种：

### 1）回调函数
异步发送完成后，通过回调拿结果。生产上更常用。

### 2）同步阻塞等待
通过 `get()` 等待发送结果，但这样会把异步发送变成同步，性能较差，通常不推荐大规模使用。

---

## Q31：Producer 为什么要配置重试？

因为很多发送失败并不是永久失败，而是瞬时故障，例如：

- 网络闪断
- Broker 短暂不可用
- Leader 切换期间抖动

配置合理的 `retries` 和重试间隔后，很多失败请求可以自动恢复成功。

---

## Q32：Producer 侧如何尽量避免消息丢失？

面试里可以答成一组组合拳：

- 发送结果要有回调确认
- 合理配置 `retries`
- 使用 `acks=all`
- 提高副本数
- 合理设置 `min.insync.replicas`
- 对关键消息做好业务补偿或重发机制

---

# 八、Consumer、Offset 与重复消费

## Q33：Kafka 消费模型是什么？

Kafka 采用的是 **Pull 模式**。  
也就是说，消费者主动向 Broker 拉取消息，而不是 Broker 主动推送消息。

Pull 模式的优点在于：

- 消费节奏由消费者自己控制
- 更容易做批量拉取
- 更适合应对不同速度的消费端
- 背压模型更清晰

---

## Q34：什么是 Consumer Group？

Consumer Group 是 Kafka 的消费组织方式。  
同一个消费组里的多个消费者，会共同消费 Topic 的不同分区，实现负载均衡。

规则是：

- 同组内，一个 Partition 同一时刻只能被一个消费者消费
- 不同组之间互不影响，可以各自完整消费同一 Topic

---

## Q35：为什么一个分区只能被同组的一个消费者消费？

这是为了避免：

- 重复消费
- Offset 冲突
- 顺序性混乱
- 处理结果不可控

因为一个 Partition 本身是一条有序日志，如果多个消费者同时处理同一分区：

- 谁先提交 offset？
- 谁算真正消费成功？
- 消费顺序还是否成立？

都会变得非常复杂。

因此 Kafka 明确约束：

> 同一消费组内，一个分区只会分配给一个消费者。

---

## Q36：什么是 offset？

offset 可以理解为消息在 Partition 中的位置编号。  
每条消息在一个 Partition 内都有唯一递增的 offset。

消费者本质上就是依靠 offset 来记录“自己读到哪里了”。

---

## Q37：offset 有什么作用？

主要有三个作用：

### 1）标识消息位置
每条消息在 Partition 内通过 offset 唯一定位。

### 2）支持断点续消费
消费者宕机重启后，可以从上次提交的 offset 继续拉取。

### 3）支持消费进度管理
Kafka 可以知道某个消费组当前消费到哪里，还落后多少消息。

---

## Q38：自动提交 offset 有什么风险？

如果开启自动提交，可能会出现：

- 消息刚拉下来
- 业务还没处理完成
- offset 已经自动提交

如果这时消费者宕机，那么这条消息实际上没处理成功，但 offset 已经前移，下次不会再拉到，于是发生**消息丢失**。

---

## Q39：为什么手动提交 offset 更安全？

因为你可以把 offset 提交放在“业务处理成功之后”。

这样可以尽量保证：

> 真正处理成功了，才认为消费完成。

不过手动提交也会带来另一个问题：

- 如果业务处理成功了
- 但 offset 还没来得及提交，消费者就挂了

那么这条消息下次会被再次拉取，从而产生**重复消费**。

---

## Q40：Kafka 为什么容易出现重复消费？

根本原因是：

> 业务处理成功 和 offset 提交成功 不是同一个原子操作。

只要这两个动作之间出现宕机、网络异常、进程退出，就可能出现重复消费或消息丢失。

因此 Kafka 更擅长提供的是：

- 最多一次
- 至少一次

而“恰好一次”通常需要额外机制配合。

---

## Q41：重复消费怎么解决？

最常见的思路不是寄希望于 Kafka 天然消灭重复，而是靠业务做幂等。

常见方案有：

- 用业务唯一键去重
- 数据库唯一索引
- Redis 记录处理标记
- 状态机版本号校验
- 消费前查重、消费后落标记

一句话总结：

> Kafka 的重复消费通常靠业务幂等兜底。

---

# 九、消息不丢失

## Q42：Kafka 会丢消息吗？

会。  
消息可能在三个环节丢失：

- Producer 侧
- Broker 侧
- Consumer 侧

这是 Kafka 面试非常高频的一道题，必须分层回答。

---

## Q43：Producer 侧为什么会丢消息？

常见原因有：

- 消息发送出去但网络中断
- Broker 没收到或没来得及响应
- Leader 切换造成写入失败
- 程序没有检查发送结果
- 重试没配好或次数耗尽

所以 Producer 侧最核心的能力是：

- 确认发送结果
- 合理重试

---

## Q44：Broker 侧为什么会丢消息？

典型场景是：

- 消息只写入 Leader
- Follower 还没来得及同步
- Leader 所在 Broker 就挂了
- 又从不同步的副本里选了新 Leader

这样旧 Leader 上那部分“来不及同步”的消息就丢了。

所以 Broker 侧的可靠性核心依赖：

- ISR
- acks=all
- 合理副本数
- 禁止 unclean leader election

---

## Q45：Consumer 侧为什么会丢消息？

典型场景是：

- 消费者刚拉到消息
- 还没真正处理完成
- offset 就已经自动提交
- 消费者宕机

这样这条消息不会再被消费，相当于业务层丢了。

所以 Consumer 侧要想更可靠，通常做法是：

> 关闭自动提交，处理成功后手动提交 offset。

---

## Q46：如何完整回答“Kafka 如何保证消息不丢失”？

可以按三层来答：

### Producer 侧
- 发送结果加回调
- 开启重试
- 关键场景用 `acks=all`

### Broker 侧
- `replication.factor >= 3`
- `min.insync.replicas > 1`
- `acks=all`
- `unclean.leader.election.enable = false`

### Consumer 侧
- 关闭自动提交
- 处理成功后手动提交 offset
- 业务层做好幂等

这套回答非常标准，也非常适合中高级面试。

---

# 十、消息积压与消费能力问题

## Q47：什么是消息积压？

消息积压是指：

> 生产速度长期高于消费速度，导致 Topic 中未消费消息不断堆积。

表面现象通常是：

- 消费延迟变大
- lag 持续增加
- 下游处理越来越慢

---

## Q48：Kafka 消息积压怎么办？

最直接的办法通常有：

### 1）增加消费者实例
提升消费能力。

### 2）增加 Partition 数量
提升同组并发上限。

但要注意：增加消费者只有在分区数足够时才有效。

---

## Q49：除了加消费者和加分区，还要排查什么？

线上处理积压不能只想着“扩容”，还要排查根因：

- 消费逻辑是不是太重
- 下游数据库 / 接口是不是变慢
- 是否出现锁争用或线程池阻塞
- 是否发生 GC 抖动
- 是否有热点分区
- 是否有异常消息反复重试

完整面试回答可以组织成：

> 先评估 lag，再判断是消费能力不足、分区并发不够，还是业务处理链路出了性能瓶颈。

---

## Q50：为什么增加消费者不一定能解决积压？

因为 Kafka 的并发上限受 Partition 数量限制。  
如果 Topic 只有 4 个 Partition，那么同组就算开 20 个消费者，也只有 4 个真正有活干。

因此：

- 消费者个数不能脱离分区数单独谈
- Kafka 的扩容常常是“分区扩容 + 消费实例扩容”一起考虑

---

# 十一、Kafka 为什么快

## Q51：Kafka 为什么性能高、吞吐大？

Kafka 的高性能主要来自几方面：

### 1）顺序写日志
Kafka 的消息写入通常是追加写，不是频繁随机写。顺序写磁盘性能比随机写高得多。

### 2）Page Cache
Kafka 充分利用操作系统页缓存，让磁盘读写更高效，很多场景下读写并不真的每次都直接命中物理磁盘。

### 3）零拷贝
Kafka 在消息传输时可借助 `sendfile` 等机制，减少用户态和内核态之间的数据拷贝以及上下文切换。

### 4）多 Partition 并发
Topic 拆成多个 Partition 后，可以并发生产和消费。

### 5）批量处理
Kafka 的发送和拉取通常不是一条一条做，而是按批处理，摊薄网络和系统调用开销。

---

## Q52：什么是零拷贝？为什么 Kafka 要用它？

传统数据传输往往要经过多次数据拷贝和多次上下文切换：

- 从磁盘读到内核缓冲区
- 拷贝到用户缓冲区
- 再拷回内核 socket 缓冲区
- 最后发给网卡

零拷贝的核心思想是：

> 尽量减少不必要的数据搬运和用户态/内核态切换。

Kafka 使用零拷贝，可以降低 CPU 消耗，提升文件传输效率，因此特别适合大吞吐消息场景。

---

## Q53：Kafka 高性能只靠零拷贝吗？

不能只答零拷贝。  
更完整的说法应该是：

- 架构层：多 Partition 并行
- 存储层：顺序写
- 系统层：Page Cache
- 传输层：零拷贝
- 协议层：批量发送 / 批量拉取

面试里只答“零拷贝”会显得视角太窄。

---

# 十二、重试、异常消息与死信队列

## Q54：消费者消费失败后怎么办？

常见做法是：

- 先做有限次数重试
- 配置重试间隔或退避策略
- 超过最大次数后转入死信队列（DLQ）

这样能避免问题消息一直阻塞主消费流程。

---

## Q55：什么是死信队列（DLQ）？

死信队列就是：

> 专门存放那些重试多次后仍然处理失败的消息的队列 / Topic。

这些消息通常不是直接丢掉，而是留给后续：

- 人工排查
- 自动补偿
- 修复后重放

---

## Q56：死信队列有什么价值？

主要价值有三点：

### 1）隔离问题消息
避免异常消息一直卡住主消费链路。

### 2）保留失败现场
方便排查到底是数据格式问题、业务逻辑问题，还是依赖服务问题。

### 3）支持补偿和重放
后续修好逻辑或数据后，可以重新处理这些消息。

---

# 十三、面试中的综合高频题

## Q57：Kafka 如何保证顺序、可靠性和高吞吐？

这是个高频综合题，可以分三部分回答：

### 顺序性
- Kafka 只能保证分区内顺序
- 同一业务键进入同一 Partition
- 同一 Partition 尽量串行消费

### 可靠性
- Producer 检查发送结果并配置重试
- Broker 侧配置 `acks=all`、合理副本数和 ISR 参数
- Consumer 手动提交 offset
- 业务层做幂等

### 高吞吐
- 多 Partition 并发
- 顺序写日志
- Page Cache
- 零拷贝
- 批量处理

最后补一句：

> 这三者往往是权衡关系，业务中通常不可能无限同时拉满。

---

## Q58：Kafka 的消费并发和线程模型，面试怎么答更像实战？

可以这样答：

> Kafka 的消费并发本质受 Partition 数量限制。同一个消费组里，一个 Partition 只能分给一个消费者，因此想提高消费并发，要么增加分区数，要么增加消费者实例，但消费者实例数不能超过分区数太多。  
> 如果单条消息处理很重，还要继续往下拆：是应用层一条线程串行处理，还是拉下来后进线程池异步执行；但如果涉及顺序消费，就不能随意并发打乱单分区处理顺序。

这类回答会比只背概念更有工程感。

---

## Q59：Kafka 和传统 MQ 的一个本质区别是什么？

一个很好的回答是：

> Kafka 更像“分布式日志平台”，而不是单纯的消息中间件。

具体体现在：

- 更强调吞吐和扩展
- 天然支持消息持久化和回放
- 通过 Partition 组织并发
- 非常适合日志、埋点、异步解耦、流处理

---

## Q60：Kafka 的核心设计思想一句话怎么总结？

可以直接背这句：

> Kafka 把消息当作分布式追加日志来管理，用 Topic 和 Partition 做分类与扩展，用副本机制保证高可用，用 offset 管理消费进度，用 Page Cache 和零拷贝提升吞吐。

---

# 十四、面试收尾总结（1 分钟版）

Kafka 是一个采用发布-订阅模型的分布式消息系统，本质上也是分布式日志系统。它的核心概念包括 Producer、Consumer、Broker、Topic 和 Partition，其中 Partition 是消息真正存储和保证分区内有序的地方。  
Kafka 通过多 Partition 提升并发能力，通过多副本和 Leader/Follower 机制提升高可用，通过 ISR、acks、`min.insync.replicas` 等机制提升消息可靠性。  
消费模型上，Kafka 采用 Pull 模式，消费者通过 offset 管理消费进度。为了避免消息丢失，通常会关闭自动提交 offset，在处理成功后手动提交；为了应对重复消费，业务上要做好幂等。  
性能方面，Kafka 结合顺序写、Page Cache、零拷贝和批量处理，具备非常强的吞吐能力。顺序性方面，它只能天然保证分区内有序；如果业务要保证更强顺序，通常需要把同一业务键路由到同一 Partition 并串行消费。

---

# 十五、背诵关键词（可做最后冲刺）

- 发布订阅模型
- Topic 是逻辑概念，Partition 是物理概念
- 分区决定并发上限
- 多分区保吞吐，多副本保高可用
- Leader 对外读写，Follower 负责同步
- ISR 是同步副本集合
- `acks=all`
- `replication.factor >= 3`
- `min.insync.replicas > 1`
- Pull 模式
- offset 管理消费进度
- 自动提交可能丢消息
- 手动提交可能重复消费
- 幂等兜底
- 分区内有序，不保证全局有序
- 顺序写、Page Cache、零拷贝、批处理
- 消息积压：加消费者、加分区、查瓶颈
- 失败重试、死信队列、补偿重放

